spring:
    application:
        name: Auth-Admin-service
    datasource:
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://localhost:5431/Admin?createDatabaseIfNotExist=true
        username: Admin
        password: TestPassword
    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: update
security:
    jwt :
        secretKey : 31bR29G5KVtBdCIspV11aPM7PnaOq8js

server:
    port: 9999

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:10000/eureka/


client:
    url:  http://localhost:8082

logging:
    level:
        org:
            springframework: DEBUG
        com:
            wholesaled : DEBUG


