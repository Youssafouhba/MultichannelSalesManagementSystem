/**
 * Checks if a file is ignored by git.
 * @param filePath The path to the file to check.
 * @returns `true` if the file is ignored by git.
 */
export declare function isFileIgnoredAsync(filePath: string): Promise<boolean>;
